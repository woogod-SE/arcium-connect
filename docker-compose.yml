version: '3.8'
services:
  arcium-app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: arcium-connect-node
    ports:
      - "8000:8000"
    restart: unless-stopped